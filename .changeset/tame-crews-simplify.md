---
"expo-app-info": patch
---

Simplify verbose source comments: compress long-form rationale in `scripts/probe-*.mjs`, `src/api.mjs`, `src/args.mjs`, and `src/cli.mjs` into shorter conclusion + why notes instead of re-explaining API behavior already documented elsewhere. Comment-only change — no change to logic or output.
