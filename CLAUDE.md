# shelfit

## 実装着手前のルール（必須）

このリポジトリでコードの実装・修正（新機能・バグ修正・リファクタ問わず）を行う前に、**必ず以下の手順を踏むこと**。ユーザーから「実装して」と明示的に指示された場合であっても、このチェックを省略しない。

1. **GitHub Issue の有無を確認する**（`my-shelfio/shelfit` リポジトリ）。
   - `search_issues` / `list_issues` で関連する Issue を検索する。
2. **Issue が無い場合**
   - その場で実装に着手しない。
   - 影響範囲（変更対象ファイル・設計方針・テスト方針など）を洗い出し、GitHub Issue として登録する。
   - Issue 作成後、ユーザーに実装に着手してよいか確認する。ユーザーの明示的な OK が出るまで実装しない。
3. **Issue が既にある場合**
   - どの Issue に対応する作業か（番号・タイトル）をユーザーに提示する。
   - 着手してよいか確認してから実装を始める。
4. 実装に着手してよいとユーザーが明示的に答えた場合のみ、コード変更・テスト追加・PR 作成などの実作業を行う。

**理由**: 過去に、確認なしで実装まで一気に進めてしまい、後から「実装は取り消してほしい、まずは Issue 登録だけでよかった」と修正を依頼されたことがある。着手判断（今やるか／Issue化だけに留めるか）はユーザー側の判断事項であり、Claude が先回りして決めてはならない。

## 関連ドキュメント

- 命名規約・ドキュメント運用など、プロダクト仕様書・ADR は [ProductDocs](../ProductDocs/CLAUDE.md) リポジトリを参照。

## レビュー・設計ルール

- 設計レビュー（Issue・PR）は [.claude/rules/design-review.md](.claude/rules/design-review.md) の観点に従う。
- 前提となる EAS API・コードベースの知識は [.claude/rules/eas-api-knowledge.md](.claude/rules/eas-api-knowledge.md) を参照。
