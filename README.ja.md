# expo-app-info

[English](./README.md) | 日本語

[![npm version](https://img.shields.io/npm/v/expo-app-info.svg)](https://www.npmjs.com/package/expo-app-info)
[![license](https://img.shields.io/npm/l/expo-app-info.svg)](./LICENSE)
[![node](https://img.shields.io/node/v/expo-app-info.svg)](https://nodejs.org)

> アカウント内のすべての Expo (EAS) アプリを、プラットフォームごとの最新ビルドバージョンとともに一覧表示 — **どのディレクトリからでも**実行できます。

> **非公式です。** Expo による提供・承認を受けたものではありません。

```
$ npx expo-app-info

┌─────────┐────────────┐────────────┐──────────┐─────────┐───────┐─────────────────────┐
│ ACCOUNT │ APP        │ SLUG       │ PLATFORM │ VERSION │ BUILD │ BUILD DATE          │
├─────────┼────────────┼────────────┼──────────┼─────────┼───────┼─────────────────────┤
│ myorg   │ Storefront │ storefront │ ios      │ 3.2.1   │ 41    │ 2026/07/26-09:12:34 │
│ myorg   │ Storefront │ storefront │ android  │ 3.2.0   │ 38    │ 2026/06/30-14:05:02 │
│ myorg   │ Field Ops  │ field-ops  │ ios      │ 1.4.0   │ 12    │ 2026/05/28-18:40:11 │
│ myorg   │ Prototype  │ prototype  │ -        │ -       │ -     │ -                   │
└─────────┘────────────┘────────────┘──────────┘─────────┘───────┘─────────────────────┘
```

## 🚀 機能

複数の Expo アプリを運用している場合、「今、どのアプリがどのバージョンか」にすぐ答える方法がありません — `eas build:list` はプロジェクトディレクトリの**内側でしか**動作せず、一度に1つのプロジェクトしか表示できません。`eas project:list` というコマンドも存在せず、Expo のダッシュボードでは各プロジェクトを1つずつクリックして確認する必要があります。`expo-app-info` は EAS GraphQL API 経由でアカウント全体を走査し、1つの表にまとめて出力します。

- アカウント内のすべての Expo (EAS) アプリを、プラットフォームごとの最新の**成功した**ビルドバージョンとともに、**どのディレクトリからでも**一覧表示
- CI やスプレッドシート向けの `--json` / `--csv` 出力
- `ios` または `android` に絞り込む `--platform` フィルター
- `--history <N>` — 最新の1件だけでなく、プラットフォームごとに直近 `N` 件のビルドを表示
- `--usage` — UTC暦月ごとの成功ビルド数（デフォルトは直近3ヶ月、`--month <n>` で最大12ヶ月まで）。ビルド履歴からクライアントサイドで算出
- `--plan` — 現在のアカウントのサブスクリプション: プラン、プランID、ステータス、同時実行数、トライアル終了日
- `ACCOUNT` は EAS の「表示名」が設定されていればそれを表示し、なければ一意な slug にフォールバック
- ランタイム依存関係ゼロ

## 📦 インストール

```bash
npx expo-app-info
```

インストールは不要です。もしくは:

```bash
npm install -g expo-app-info
expo-app-info
```

Node.js **20以降**が必要です（この CLI はグローバルの `fetch` を使用します）。Node 20 は2026年4月にサポート終了(EOL)を迎えたため、Node 22 LTS 以降を推奨します。

## 🔑 認証

**`EXPO_TOKEN`** 環境変数に設定したパーソナルアクセストークン — これが唯一サポートされる認証情報です。

```bash
export EXPO_TOKEN=xxxxxxxx
npx expo-app-info
```

[expo.dev/settings/access-tokens](https://expo.dev/settings/access-tokens) で作成できます。

これは意図的に唯一の選択肢としています。トークンは `argv` から読み取られることも、ディスクに書き込まれることもないため、シェル履歴・プロセス一覧・置き忘れた設定ファイルを経由して漏洩することがありません。`EXPO_TOKEN` が未設定の場合、CLI は非ゼロのステータスで終了します — プロンプトで待機することはなく、CI 上での実行も安全です。

## 🛠️ 使い方

プラットフォームで絞り込んだり、スクリプト向けに出力形式を切り替えたりできます:

```bash
npx expo-app-info --platform ios
npx expo-app-info --json  > apps.json
npx expo-app-info --csv   > apps.csv
```

プラットフォームごとの最新ビルドだけでなく、それ以上を表示する:

```bash
npx expo-app-info --history 5
```

```
┌─────────┐────────────┐────────────┐──────────┐─────────┐───────┐─────────────────────┐
│ ACCOUNT │ APP        │ SLUG       │ PLATFORM │ VERSION │ BUILD │ BUILD DATE          │
├─────────┼────────────┼────────────┼──────────┼─────────┼───────┼─────────────────────┤
│ myorg   │ Storefront │ storefront │ ios      │ 3.2.1   │ 41    │ 2026/07/26-09:12:34 │
│ myorg   │ Storefront │ storefront │ ios      │ 3.2.0   │ 40    │ 2026/06/30-14:05:02 │
│ myorg   │ Storefront │ storefront │ android  │ 3.2.0   │ 38    │ 2026/06/30-14:03:47 │
└─────────┘────────────┘────────────┘──────────┘─────────┘───────┘─────────────────────┘
```

`--history <N>`（1〜100、デフォルト: 1）は、各アプリ/プラットフォームを1行に集約する代わりに、プラットフォームごとに直近 `N` 件の**成功した**ビルドを別々の行として出力します。行は常にビルド日時の新しい順にソートされます — API のレスポンス順をそのまま信用するのではなく、クライアント側でソートしています。API の返却順はどこにも文書化されていないためです。`--platform` / `--json` / `--csv` と併用できますが、`--usage` とは組み合わせられません。`--history 1` はフラグを付けない場合とまったく同じ出力になります。

アプリのバージョンではなく、暦月ごとの成功ビルド数を確認することもできます:

```bash
npx expo-app-info --usage
```

```
┌─────────┬─────────────────────────┬──────────────────────────┬──────────────────────────┐
│ ACCOUNT │ PERIOD                  │ SUCCESSFUL BUILDS (IOS)  │ SUCCESSFUL BUILDS (AND)  │
├─────────┼─────────────────────────┼──────────────────────────┼──────────────────────────┤
│ myorg   │ 2026-07-01 → (today)    │ 18                       │ 16                       │
│ myorg   │ 2026-06-01 → 2026-06-30 │ 14                       │ 15                       │
│ myorg   │ 2026-05-01 → 2026-05-31 │ 21                       │ 20                       │
└─────────┴─────────────────────────┴──────────────────────────┴──────────────────────────┘
```

1行がアカウントごと・**UTC暦月ごと**に対応し、デフォルトでは直近3ヶ月分を表示します。`--month <n>`（1〜12）を渡すとウィンドウを広げられます。例: `--usage --month 6` で直近半年分。`SUCCESSFUL BUILDS` の件数は API 経由のビルド履歴からクライアントサイドで算出したものであり — 請求サイクルに紐づき任意の暦月範囲には分割できない、EAS 自身の課金/使用量メトリクスから読み取ったものではありません — そのため、EAS ダッシュボードの数値と厳密には一致しない場合があります。`--platform ios` または `--platform android` を渡すと、そのプラットフォームの列だけに絞り込めます。集計中の当月の行は、まだ確定した件数ではないため、終端を `(today)` と表示します。

あるいは、ビルド件数や請求期間を含めず、アカウントの現在のサブスクリプションだけを確認することもできます:

```bash
npx expo-app-info --plan
```

```
┌─────────┐────────────┐────────────┐────────┐─────────────────────────────┐───────────┐
│ ACCOUNT │ PLAN       │ PLAN ID    │ STATUS │ CONCURRENCY (TOTAL/IOS/AND) │ TRIAL END │
├─────────┼────────────┼────────────┼────────┼─────────────────────────────┼───────────┤
│ myorg   │ Production │ production │ active │ 2 / 1 / 1                   │ -         │
└─────────┘────────────┘────────────┘────────┘─────────────────────────────┘───────────┘
```

`--plan` はアカウントの**現在の**サブスクリプションのみを表示し、ビルド件数も請求期間も含みません — これらは「現時点の」事実であり、過去のデータと並べて表示すると各行に同じ値が繰り返されるだけになってしまうためです。`--platform ios` または `--platform android` を渡すと、`CONCURRENCY` をそのプラットフォームの数値だけに絞り込めます。`--usage` や `--history` とは組み合わせられません。3つとも独立した表示モードだからです。

### 数値の意味

| 列             | 出典                                                                                 |
| -------------- | -------------------------------------------------------------------------------------- |
| `ACCOUNT`      | アカウントの EAS「表示名」が設定されていればそれ、なければ一意な slug（テーブル表示のみ — 下記参照） |
| `APP` / `SLUG` | EAS のプロジェクト名と slug                                                              |
| `PLATFORM`     | `ios` / `android`                                                                      |
| `VERSION`      | 最新の**成功した**ビルドの `appVersion`                                        |
| `BUILD`        | `appBuildVersion`（iOS のビルド番号 / Android の versionCode）                             |
| `BUILD DATE`   | そのビルドが完了した日時（`YYYY/MM/DD-HH:mm:ss`、UTC）                                  |

`--usage` の場合、代わりにアカウントごと・**UTC暦月ごと**の1行になります（デフォルトは直近3ヶ月、`--month <n>` で1〜12ヶ月まで）:

| 列                        | 出典                                                                                                                                                                                                                                                |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ACCOUNT`                 | 上記と同様 — アカウントの EAS「表示名」が設定されていればそれ、なければ一意な slug                                                                                                                                                                          |
| `PERIOD`                  | UTC暦月。テーブルでは集計中の当月の終端を `(today)` と表示し、それ以外は月の最終日（inclusive）を表示。`--json`/`--csv` の `periodStart`/`periodEnd` は常に暦月境界の生値（`periodEnd` は排他的境界）。 |
| `SUCCESSFUL BUILDS (IOS)` | その月に完了した iOS ビルド数。API 経由のビルド履歴からクライアントサイドで算出 — EAS 自身の課金/使用量メトリクスではない                                                                                                                        |
| `SUCCESSFUL BUILDS (AND)` | Android についても同様                                                                                                                                                                                                                                    |

`--platform ios` または `--platform android` を渡すと、そのプラットフォームの列だけを表示します。

`--plan` の場合、代わりにアカウントごとの1行になり、現在のサブスクリプションのみを表示します（ビルド件数・請求期間は含みません）:

| 列                        | 出典                                                                                      |
| ----------------------------- | ------------------------------------------------------------------------------------------- |
| `ACCOUNT`                     | 上記と同様                                                                               |
| `PLAN`                        | `subscription.name`                                                                         |
| `PLAN ID`                     | `subscription.planId`                                                                       |
| `STATUS`                      | Expo が報告するサブスクリプションのステータス（`active`、`trialing` など）                            |
| `CONCURRENCY (TOTAL/IOS/AND)` | 3つの同時実行数を一度に表示。`--platform` で1つに絞り込み可能                     |
| `TRIAL END`                   | `subscription.trialEnd`（`YYYY-MM-DD`）。アカウントがトライアル中でない（または一度もトライアルしていない）場合は `-` |

月額料金の列はまだありません — EAS のスキーマに存在するかどうかがまだ確認できていないためです。実トークンで確認できれば、後日追加される可能性があります。

**`VERSION` はローカルの `app.json` から読み取られたものではありません。** EAS はプロジェクト自体にバージョンを保存しないため、表示される番号は直近の成功ビルドに焼き込まれたものです。一度もビルドされていないアプリは `-` と表示されます。

**`ACCOUNT` は表示上のものにすぎません — テーブル表示のみです。** アカウントに EAS の「表示名」が設定されていればそれを表示し、なければ一意な slug にフォールバックします。表示名の方が読みやすい一方、一意性は保証されないためです。`--json`/`--csv` は、スクリプトが一意なキーとして利用できるよう、常に `account` フィールドに slug を出力します。

### フィルタリング

- `--platform <ios|android>` — このプラットフォームのビルドのみ。このフィルターを設定すると、ビルドがゼロのアプリは特定のプラットフォームに一致しないため除外されます。`--usage` と併用すると `SUCCESSFUL BUILDS` 列がそのプラットフォームに絞り込まれ、`--plan` と併用すると `CONCURRENCY` がアカウント合計ではなくそのプラットフォームの数値に絞り込まれます。
- `--month <n>` — `--usage` との併用時のみ: ウィンドウを直近 `n` 暦月（1〜12、デフォルト3）まで広げます。
- `--history <N>` — 最新の1件だけでなく、プラットフォームごとに直近 `N` 件（1〜100）の成功ビルドを新しい順に表示します。`--usage` や `--plan` とは組み合わせられません。

`--usage`、`--plan`、`--history` は互いに排他的な表示モードです — このうち2つ以上を組み合わせると `CliError` になります。

現時点で `--account` フィルターはありません — 新しい `ACCOUNT` 表示名の挙動と共存させるのではなく削除されました（[#22](https://github.com/eas-flow/expo-app-info/issues/22) 参照）。あるべき形（slug・表示名・両方）が固まれば、アカウントによるフィルタリングが復活する可能性があります。

### 機械可読な出力（`--json` / `--csv`）

どちらも、テーブルに表示されるのと同じ1行につき1エントリを、表示用文字列ではなく生の値で出力します — テーブルで `-` と表示される箇所は `null`（JSON）/ 空セル（CSV）になり、テーブルの整形済みビルド日時の代わりに完全な ISO 8601 タイムスタンプ（`lastBuildAt`）になります:

```bash
$ npx expo-app-info --json
[
  {
    "account": "myorg",
    "app": "Storefront",
    "slug": "storefront",
    "platform": "ios",
    "version": "3.2.1",
    "build": "41",
    "lastBuildAt": "2026-07-26T09:12:00.000Z"
  }
]
```

```bash
$ npx expo-app-info --csv
account,app,slug,platform,version,build,lastBuildAt
myorg,Storefront,storefront,ios,3.2.1,41,2026-07-26T09:12:00.000Z
```

`--json` と `--csv` は互いに排他的で、どちらも `--platform` と組み合わせられます。

`--usage` の場合、代わりにアカウント×月ごとに1エントリを出力します: `account`、`buildsIos`、`buildsAndroid`、`periodStart`、`periodEnd`。`buildsIos`/`buildsAndroid` は `--platform` に関係なく常に両方出力されます — このフラグが変えるのは、人間向けテーブルがどの列を表示するかだけです。`periodStart`/`periodEnd` は、集計中の当月であっても常に暦月境界の生値（`periodEnd` は排他的境界）です — `(today)` の表示はテーブルのみのものです。

`--plan` の場合、以下を出力します: `account`、`plan`、`planId`、`status`、`concurrencyTotal`、`concurrencyIos`、`concurrencyAndroid`、`trialEnd`。3つの同時実行数フィールドは `--platform` に関係なく常にすべて出力されます — このフラグが変えるのは、人間向けテーブルの `CONCURRENCY` にどの数値を表示するかだけです。

### 出力の安定性

デフォルトのテーブル（列・文言・色・間隔）は人間向けであり、互換性の保証は**一切ありません** — どのリリースでも変更される可能性があります。

`--json` と `--csv` はスクリプト向けであり、semver に従います: 既存のフィールドはメジャーバージョンアップなしにリネームや削除されることはなく、その意味も変わりません。新しいフィールドはマイナーリリースで追加される場合があります。スクリプトは認識できないフィールドを無視するべきです。

## 📚 ドキュメント

### 仕組み

`https://api.expo.dev/graphql` に対する3つの GraphQL クエリ:

1. `meActor { accounts }` — トークンが参照できるすべてのアカウント（テーブルの `ACCOUNT` 列にのみ使われる各アカウントの `displayName` を含む）
2. `account.byId(...).appsPaginated(first: 100)` — アカウントごとのアプリ、カーソルベースのページネーション
3. `app.byId(...).builds(limit, filter: { platform, status: FINISHED })` — プラットフォームごとの直近 `N` 件のビルド（`--history` を指定しない限り `limit` は1）。レスポンスはクライアント側で `createdAt` の降順にソートされます。API の返却順自体は文書化されていないためです。

ビルドクエリは同時実行数8で実行されます。ランタイム依存関係はゼロです。

`--usage` もアカウント→アプリ（ステップ1〜2）を辿りますが、ステップ3の代わりに各アプリの `app.byId(...).builds(offset, limit, filter: { status: FINISHED })` をページングし、クライアント側でプラットフォーム＋UTC暦月ごとにビルドをバケット集計します（`countBuildsByMonth`）。`subscription`・`billingPeriod`・`usageMetrics` は一切クエリしません — これらは EAS の請求サイクルに紐づいており任意の暦月範囲には分割できず、また `metricsForServiceMetric` の `filterParams` は実際にはプラットフォームで絞り込めない（渡した内容にかかわらず常に合算値を黙って返す）ことが判明しているためです。アカウント、およびアカウント内の各アプリも同時実行数8で取得されます。UTC暦月の境界には期間間の依存関係がないためです（前の期間の `start` を知ってから次を計算する必要があった旧来の請求期間チェーンとは異なります）。

`--plan` はステップ2・3を完全にスキップし、アカウントごとにより小さなクエリを実行します: `account.byId(...) { subscription }` のみ — `billingPeriod` や `usageMetrics` は含みません。各アカウントのサブスクリプション取得は互いに独立しているため、アカウントは並列（同時実行数8、ビルドクエリと同じ）で取得されます。

### さらに詳しく

- [CONTRIBUTING.md](./CONTRIBUTING.md)（[日本語版](./CONTRIBUTING.ja.md)） — 開発環境のセットアップ、テスト/lint コマンド、プロジェクト構成、リリースプロセス
- [SECURITY.md](./SECURITY.md)（[日本語版](./SECURITY.ja.md)） — 脆弱性報告のポリシーと、非公開で問題を報告する方法
- [CODE_OF_CONDUCT.md](./CODE_OF_CONDUCT.md)（[日本語版](./CODE_OF_CONDUCT.ja.md)） — このプロジェクトが従う Contributor Covenant
- 下記のロードマップ — 実装済み・予定の機能

## 🗺️ ロードマップ

- [x] CI やスプレッドシート向けの `--json` / `--csv` 出力
- [x] `--platform` フィルター
- [x] `--usage`: サブスクリプションプラン、ビルド同時実行数、プラットフォームごとの月間ビルド数 ([#15](https://github.com/eas-flow/expo-app-info/issues/15))
- [x] `--history <N>`: 最新の1件だけでなく、プラットフォームごとに直近 N 件のビルドを表示 ([#17](https://github.com/eas-flow/expo-app-info/issues/17))
- [x] `ACCOUNT` は EAS の「表示名」を表示（slug にフォールバック）。`--account` は削除 ([#22](https://github.com/eas-flow/expo-app-info/issues/22))
- [x] `--plan`: `--usage` のビルド件数とは独立した、現在のアカウントのサブスクリプション（プラン、プランID、ステータス、同時実行数、トライアル終了日） ([#19](https://github.com/eas-flow/expo-app-info/issues/19))
- [x] `--usage`: アカウントごと・UTC暦月ごとの1行（デフォルトで直近3ヶ月、`--month <n>` で最大12ヶ月）。EAS の請求期間メトリクスの代わりにクライアントサイドの「成功ビルド」件数を使用 ([#18](https://github.com/eas-flow/expo-app-info/issues/18))
- [ ] ローカルの `app.json` との差分表示で、ソースと出荷済みビルドのバージョンのずれを可視化
- [ ] ビルドバージョンと並べて、最新のストア提出バージョンを表示

Issue や PR を歓迎します。

## ❓ FAQ

**これは公式の Expo ツールですか？**

いいえ。EAS GraphQL API は**公式に文書化・バージョン管理されていません**。フィールド名は Expo 自身のオープンソースクライアント（[`eas-cli`](https://github.com/expo/eas-cli)、[`orbit`](https://github.com/expo/orbit)）から推測したものであり、予告なく変更される可能性があります。本プロジェクトは Expo による提供・承認を受けたものではありません。

**パーソナルアクセストークンの代わりにロボットトークンを使えますか？**

ロボットトークンは、それを発行したアカウントしか参照できません。所属するすべてのアカウントを一覧表示するには、パーソナルアクセストークンを使用してください。

**トークンがアカウントのアプリやビルドを読み取れない場合はどうなりますか？**

`--usage` はもはや請求スコープのフィールドを一切クエリしません — ビルド件数は各アプリのビルド履歴からクライアントサイドで算出されます。あるアカウントのアプリやビルドの取得が何らかの理由で失敗しても、そのアカウントの行は `-`（`--json`/`--csv` では `null`）付きで出力され続け、理由は stderr に出力されます — 実行全体が失敗することはありません。

**なぜ一部のアカウントは `--plan` の列で `-` と表示されるのですか？**

プランデータは請求スコープです。トークンにアカウントの請求権限がない場合、その行はプラン列に `-` を表示したまま出力され、理由は stderr に出力されます。実行全体を失敗させることはありません。

**`VERSION` はローカルの `app.json` から取得されますか？**

いいえ。EAS はプロジェクト自体にバージョンを保存しないため、表示される番号は直近の成功ビルドに焼き込まれたものです。一度もビルドされていないアプリは `-` と表示されます。

**`EXPO_TOKEN` がディスクに書き込まれたりログに出力されたりすることはありますか？**

ありません。`argv` から読み取られることも、ディスクに書き込まれることも、出力されることもありません。ポリシーの詳細と脆弱性を非公開で報告する方法については [SECURITY.md](./SECURITY.md) を参照してください。

## 📄 ライセンス

[MIT](./LICENSE)
